# Production: set to your deployed backend URL (no trailing slash).
# Build with: npm run build (uses this if you copy to .env.production)
# Example: VITE_API_URL=https://your-api.onrender.com
VITE_API_URL=https://your-backend-url.com
