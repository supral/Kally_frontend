# Backend API URL (no trailing slash). Frontend calls ${VITE_API_URL}/api/...
# For production: set VITE_API_URL to your deployed backend (e.g. https://your-api.onrender.com)
# Use .env.production or .env.production.example when building for deploy.
VITE_API_URL=http://localhost:5000
